#include "main.h"
/* more headers goes there */

/* betty style doc */

/**
 * _abs - Entry point
 *
 * @n: is the char to be checked
 *  Return: Always 0 (success)
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
